<!DOCTYPE html>
<html>
  <head>
    <title>No Scrubs Movie Reviews Entertainment Productions</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <%= link_to 'All Movies', root_path %>
    <% if current_user %>
      <%= link_to "#{current_user.username}", user_path(current_user) %>
      <button id="logout-button"><%= link_to 'Logout', logout_path %></button>
    <% else %>
      <button id="login-button"><%= link_to 'Login', login_path %></button>
      <button id="register-button"><%= link_to 'Sign Up', new_user_path %></button>
    <% end %>
    <h1>No Scrub Movie Review</h1>
    <h2>Entertainment Productions</h2>

    <%= yield %>

  </body>
<!-- <footer class="footer">Couldn't happen without our boy, Vince</footer>
 -->
</html>
